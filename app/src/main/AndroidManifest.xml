<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lobxy.achs">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login" />
        <activity android:name=".Register" />
        <activity android:name=".User.Activities.UserMainScreen" />
        <activity android:name=".User.Activities.ComplainForm" />
        <activity android:name=".User.Activities.UserComplainActivity" />
        <activity android:name=".User.Activities.Profile" />
        <activity android:name=".User.Activities.Complains" />
        <activity android:name=".Admin.AdminMainScreen"></activity>
        <activity android:name=".Admin.ComplaintActivity" android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".Admin.AddSupervisor" />
        <activity android:name=".Admin.ListSupervisor" />
        <activity android:name=".Supervisor.SupervisorDetail" />
        <activity android:name=".Supervisor.SupervisorMain" />


    </application>

</manifest>